# AI Research Digest - esempio demo

Questo file mostra il formato atteso dalla demo offline. Per rigenerarlo localmente:

```bash
python main.py --demo
```

Il digest reale viene scritto nella directory configurata; la demo usa `out/` per default.
